<controls1:WindowEx x:Class="Updater.Views.MainWindow"
                    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    xmlns:local="clr-namespace:Updater"
                    xmlns:controls="clr-namespace:Updater.Controls"
                    xmlns:controls1="clr-namespace:Updater.Controls"
                    xmlns:viewModels="clr-namespace:Updater.ViewModels"
                    WindowStartupLocation="CenterScreen"
                    WindowStyle="None"
                    Style="{StaticResource SimpleWindowEx}"
                    mc:Ignorable="d"
                    ResizeMode="NoResize"
                    Title="" Height="120" Width="400" Icon="/updater.ico">
    <controls:WindowEx.DataContext>
        <viewModels:MainViewModel />
    </controls:WindowEx.DataContext>
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding Path=Title}" VerticalAlignment="Bottom" Margin="0,3" />
        <ProgressBar Grid.Row="1" Maximum="100" Value="{Binding Percent}" />
    </Grid>
</controls1:WindowEx>